import 'package:flutter/material.dart';

class AppColors {
  // Background (slightly richer than white)
  static const background = Color(0xFFF4F6FA);

  // Cards (soft elevated)
  static const card = Color(0xFFFFFFFF);
  static const elevated = Color(0xFFF0F3FF);

  // Primary
  static const primary = Color(0xFF4F6EF7);

  // Soft accents (VERY IMPORTANT)
  static const accentBlue = Color(0xFF5FD4FF);
  static const accentPurple = Color(0xFF8E7CFF);
  static const accentPink = Color.fromARGB(255, 10, 166, 153);

  // Text
  static const textPrimary = Color(0xFF171717);
  static const textSecondary = Color(0xFF7A7A7A);

  // Border
  static const border = Color(0xFFE6E8EF);

  // Status
  static const positive = Color(0xFF2E9B6F);
  static const negative = Color.fromARGB(255, 107, 217, 213);
}